package com.newlecture.study.scenario;

public class sss {
	public static void main(String[] args) {

		int target = 10;
		int x;
		int y;
		int z;
		
		
		int perHitTriple;
		int perHitDouble;
		int perHitSingle;
		int perHitMiss;
//		
//		100-x = perHitTriple;
//		perHitTriple = ((100 - (target * 3))/2);
//		perHitDouble = (100 - (target * 2));
//		perHitSingle = (100 - (target * 1));
//		perHitMiss = (100 - (target * 0));
		
		(100 <= per && per <=x);
		( x< per && per <=y);
		( y< per && per <=z);
		( z< per && per <=1);
		perHitTriple = 20;
		perHitDouble = 30;
		perHitSingle =  ;
		perHitMiss = target*2;
//				
//		
		x=100-perHitTriple;
		y=(x-1)-perHitDouble;
		z=(y-1)-perHitSingle;
		perHitMiss = (z-1) -1;
		
//		x= ( (target/20)*);
//		y= x-target*2;
//		z=y-target;
		
		
//		int perHitTriple = 100-x;
//		int perHitDouble = (x-1) - y;
//		int perHitSingle = (y-1) - z;
//		int perHitMiss = (z-1) -1;

		System.out.printf("(100 <= tripeZone && tripeZone <=%d)\n", x);
		System.out.printf("(%d-1) <= doubleZone && doubleZone <=%d)\n", x,y);
		System.out.printf("(%d-1) <= singleZone && singleZone <=%d)\n", y, z);
		System.out.printf("(%d-1) <= missZone && missZone <=1)\n", z);
		
		System.out.println(perHitTriple );
		System.out.println(perHitDouble);
		System.out.println(perHitSingle);
		System.out.println(perHitMiss);

	}
}
